# Polyglot code-intelligence E2E fixture

This Markdown file and the adjacent Python module verify that the public index command handles more than one configured source language in a clean disposable repository.
