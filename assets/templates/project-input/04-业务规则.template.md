# 业务规则

## 规则总览

| rule_id | 规则名称 | 类型 | 关联能力 capability_id | 关联流程 process_id | 关联节点 node_id | 关联实体 entity_id | 是否可配置 | 是否 DMN 候选 | evidence_id | question_id |
|---|---|---|---|---|---|---|---|---|---|---|
| RULE-XXX-001 | | 路由/校验/计算/分配/审批 | CAP-xxx | PROC-xxx | ND-xxx | ENT-xxx | 是/否 | 是/否 | EV-xxx | Q-xxx |
| RULE-XXX-002 | | | | | | | | | | |

> 规则类型：路由 / 校验 / 计算 / 分配 / 审批 / 生成 / 留存
> 编号前缀固定为 `RULE-`（不用 `R-`），以与 `ROLE-` / `REL-` 彼此无前缀包含关系，便于机械校验。

---

## 规则详细描述

（每条规则复制以下模板填写）

### RULE-XXX-001 规则名称

- **规则类型**：（路由 / 校验 / 计算 / 分配 / 审批 / 生成 / 留存）
- **关联能力**：（capability_id）
- **关联流程**：（process_id）
- **关联节点**：（node_id）
- **关联实体**：（entity_id）
- **触发时机**：（什么时候执行这条规则）
- **证据来源**：（evidence_id）
- **待确认问题**：（question_id；无则写“无”）

**输入条件**：

| 条件字段 | 来源 | 运算符 | 值/范围 |
|----------|------|--------|---------|
| | | | |

**输出结果**：

| 输出字段 | 值/动作 | 说明 |
|----------|---------|------|
| | | |

**规则逻辑（伪代码或自然语言）**：

```
IF 条件A AND 条件B THEN 结果1
ELSE IF 条件C THEN 结果2
ELSE 默认结果
```

**异常处理**：

- 输入缺失时：
- 条件冲突时：

**是否可配置**：是/否（若可配置，说明配置项和默认值）

---

### RULE-XXX-002 规则名称

（同上格式）
