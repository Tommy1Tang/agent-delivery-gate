# 输入缺口清单

> 本文件由 `scripts/validate_input_contract.py --gap-list-out docs/输入缺口清单.md` 自动生成。
> 手工补充说明时，请只在「补充说明」章节追加，不要改写机械校验结论。
> 门禁 verdict = partial 或 fail 时，本文件为**必需交付物**（`validate_delivery.py` 会检查）。

- 门禁结论：**通过 / 部分通过 / 不通过**
- 输入目录：`docs/input`
- 校验工具：`scripts/validate_input_contract.py`（机械校验，非人工判断）
- 后续动作：（由脚本写入）

## 能力完整性概览

| 指标 | 值 |
|------|----|
| 能力总数 | |
| Must 级能力 | |
| Must 完整 | |
| Must 不完整 | |

## 阻塞项（必须补齐后才能生成 PRD）

（脚本写入；包含缺失必需文件、追溯链断裂等机械可判定项）

## Must 级缺口（影响 PRD 完整性）

（脚本写入；包含能力要素缺失、实体唯一标识缺失、状态机要素缺失等）

## 提示项（建议补充，不阻塞）

（脚本写入；包含建议提供的文件、孤立编号等）

## 处理要求

1. 阻塞项必须由上游补齐后重跑本脚本，AI 不得自行补全。
2. Must 级缺口未关闭前，对应 capability 不得进入 PRD 正文。
3. 任何 AI 推断必须在 PRD 中标注「AI 推断」。
4. 高阻塞待确认问题必须进入 PRD「待确认项」并给出 mock/暂缓策略。

## 补充说明（人工填写，可选）

| 缺口项 | 责任人 | 计划关闭时间 | 临时处理方式 |
|--------|--------|-------------|-------------|
| | | | |
